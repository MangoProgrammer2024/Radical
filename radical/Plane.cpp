#include "includes/brush.h"

int c_nPlaneId = 0;

plane_t * NormalPlane(plane_t * p){
    vec_t nx;
    vec_t ny;
    vec_t nz;
     int i;
       for(i = 0; i >= 0; i++){
         printf("%f", "%5.2f, %5.2f, %5.2f",
           nx = 0.5f, ny = 1.5f, nz = -1.0f, p->dist <= nx/2);
        };
};
